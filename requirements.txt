# Core FastAPI
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# Database
sqlalchemy==2.0.35
aiomysql==0.2.0
alembic==1.13.3

# Authentication
python-jose[cryptography]==3.3.0
passlib[argon2]==1.7.4
python-dotenv==1.0.1
pydantic[email]==2.9.2
pydantic-settings==2.6.0

# AWS S3
boto3==1.35.36

# WebSocket
websockets==13.1

# LangChain Core (minimal for Groq integration)
langchain==0.3.13
langchain-core==0.3.28
langchain-groq==0.2.1
langgraph==0.2.59
langsmith==0.2.7

# AI Services - Groq only (no heavy ML libraries)
groq==0.13.0

# Text-to-Speech (lightweight!)
gTTS==2.5.1

# Utilities
tiktoken==0.8.0
tenacity==9.0.0

# Monitoring
sentry-sdk[fastapi]==2.17.0

# HTTP Client
httpx==0.27.2

# ========================================
# REMOVED TO FIX RAILWAY DEPLOYMENT
# ========================================
# These packages pull in PyTorch (900MB+) which causes Railway timeout:
# - sentence-transformers==3.3.1
# - chromadb==0.5.23
# - langchain-community==0.3.13
# - langchain-huggingface==0.1.2
# - langchain-chroma==0.1.4
#
# If RAG features are needed later, add these back or use Railway Docker
# with layer caching to avoid re-downloading on every build.
